    const today = new Date()
    const targetDate = new Date(today.getFullYear()+1, today.getMonth(), today.getDay())
/*      const tasks = [
        {id:1,description:"Fazer curso SpringBoot", isDone: false, date: targetDate},
        {id:2, description:"Fazer curso SQL", isDone: false, date: targetDate}
    ]  */